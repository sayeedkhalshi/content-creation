Hi sir, I have developed(theoretical) an education system based some hypothesis I have written, where all the theories behind all the features will be evaluated by a manipulation checking framework, debating, voting and will be upgraded like versioning, the same way we improve software algorithms. While implementing it as an app, I saw the need of a truly free web. So, I am learning cryptography/mpc for last 3-4 months.

I wanted see if I can develop a dynamic consensus and I found mysteceti is that marvel of a blockchain. Then I wanted to use that for a ledger that can be used as a database. But that would be very costly for paid RPC. My idea to this problem is having a micro sui like node installed inside the app so user can have a free RPC.

Then for storage (only text, no plan for media/images), I want to have storage (ipfs or walrus like) attached to it each app when installed on a computer. User can save and select which one to make public or encrypted or to broadcast in the blockchain or just keep on their own node only.

Now, I am really greedy in idea making and seeing if we could have a web server mechanism attached to it. The idea is, if smart contracts can have features that would let the app render pages dynamically by reading a frontend version of the contract and possibly a backend version to let us use logic or maybe private logic. Then all could be served without a server by just rendering the smart contract on a UI, since data request can be done using the rpc.

I am really new to cryptography, I am asking you if this can be done in any way. I have learned a few parts Ika's 2pc mpc codebase and theories to solve authentication mechanism. But still a long way to go.

About the education system, it's built on a lot of theories. A overview is written here, but I guess you will have a lot of question https://github.com/sayeedkhalshi/content-creation/blob/main/true-free-web/concept.md

My request, please don't just say it's dumb, I can answer any question. It's something I have given my life to, so it has to be done in anyway possible.
